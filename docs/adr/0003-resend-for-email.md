# Resend for transactional email

Chose Resend as the email sending provider for the KB platform. Alternatives considered: Nodemailer (self-managed SMTP, more setup), SendGrid (heavier SDK, pricing). Resend offers a simple TypeScript SDK, generous free tier, and verified domain support via `mail.keluargabahagia.id`.
